<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="ru.drom.gitgrep.MainActivity"
    tools:ignore="AlwaysShowAction">
    <item
        android:id="@+id/menu_main_search"
        android:icon="@drawable/ic_search_white_24dp"
        android:title="@string/search"
        android:actionLayout="@layout/include_search_view"
        android:showAsAction="always|collapseActionView"
        app:actionLayout="@layout/include_search_view"
        app:showAsAction="always|collapseActionView"
        tools:ignore="AppCompatResource"
        />

    <item
        android:id="@+id/menu_main_login"
        android:title="@string/auth"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/menu_main_logout"
        android:title="@string/deauth"
        app:showAsAction="never"
        />
</menu>